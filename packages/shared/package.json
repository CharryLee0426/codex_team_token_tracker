{
  "name": "@codex-tracker/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared parsing, pricing, aggregation and time utilities for the Codex token tracker",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "echo 'shared: source package, nothing to build'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "~5.9.3"
  }
}
